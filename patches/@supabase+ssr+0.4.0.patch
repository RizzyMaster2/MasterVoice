diff --git a/node_modules/@supabase/ssr/dist/index.mjs b/node_modules/@supabase/ssr/dist/index.mjs
index 289753a..5122f98 100644
--- a/node_modules/@supabase/ssr/dist/index.mjs
+++ b/node_modules/@supabase/ssr/dist/index.mjs
@@ -26,7 +26,11 @@ class Supabase {
     this.supabaseUrl = supabaseUrl;
     this.supabaseKey = supabaseKey;
     this.options = options;
-    const isPlatform = typeof process !== "undefined" && process.env && process.env.NEXT_RUNTIME === "edge";
+    const isPlatform =
+      typeof process !== "undefined" &&
+      process.env &&
+      // @ts-ignore
+      process.env.NEXT_RUNTIME === "edge";
     const _isServer = isBrowser ? false : typeof Deno !== "undefined" ? true : typeof process !== "undefined" && !process.versions?.electron && process.version ? true : false;
     if (!isPlatform && !_isServer) {
       this.realtime = new RealtimeClient(this.realtimeUrl, {
diff --git a/node_modules/@supabase/ssr/dist/index.cjs b/node_modules/@supabase/ssr/dist/index.cjs
index 21ac192..d524855 100644
--- a/node_modules/@supabase/ssr/dist/index.cjs
+++ b/node_modules/@supabase/ssr/dist/index.cjs
@@ -37,7 +37,11 @@ class Supabase {
     this.supabaseUrl = supabaseUrl;
     this.supabaseKey = supabaseKey;
     this.options = options;
-    const isPlatform = typeof process !== "undefined" && process.env && process.env.NEXT_RUNTIME === "edge";
+    const isPlatform =
+      typeof process !== "undefined" &&
+      process.env &&
+      // @ts-ignore
+      process.env.NEXT_RUNTIME === "edge";
     const _isServer = isBrowser ? false : typeof Deno !== "undefined" ? true : typeof process !== "undefined" && !process.versions?.electron && process.version ? true : false;
     if (!isPlatform && !_isServer) {
       this.realtime = new realtimeJs.RealtimeClient(this.realtimeUrl, {
